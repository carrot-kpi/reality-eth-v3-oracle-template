{
    "name": "@carrot-kpi/reality-eth-v3-oracle-template",
    "version": "0.1.0",
    "description": "A Carrot template.",
    "license": "GPL-3.0",
    "scripts": {
        "build": "turbo run build",
        "build:contracts": "pnpm build --filter contracts",
        "lint": "turbo run lint",
        "format": "turbo run format",
        "start": "carrot-scripts start",
        "test": "turbo run test",
        "commitlint": "commitlint -e",
        "prepare": "husky install"
    },
    "devDependencies": {
        "@commitlint/cli": "^17.1.2",
        "@commitlint/config-conventional": "^17.1.0",
        "carrot-scripts": "0.11.0",
        "husky": "^8.0.1",
        "prettier": "^2.8.1",
        "turbo": "^1.6.3"
    }
}
